{
  "name": "cloud-harness",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "pwsh -NoLogo -NoProfile -Command \"git --version; node --version; pwsh -Version; terraform version; aws --version; codex --version\"",
  "start": "sudo service docker start || true"
}
