{
  "name": "cloud-harness",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "agentCanUpdateSnapshot": true,
  "install": "pwsh -NoLogo -NoProfile -File .cursor/verify-toolchain.ps1",
  "start": "sudo service docker start || true"
}
