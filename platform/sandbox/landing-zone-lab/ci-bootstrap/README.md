# Retired CI bootstrap

The Project A GitHub OIDC roles were removed after verified retirement on
2026-09-06. The account-wide GitHub OIDC provider was intentionally preserved
because it is shared infrastructure. This directory no longer contains a
deployable Terraform root; the prior implementation remains in Git history as
portfolio evidence.
