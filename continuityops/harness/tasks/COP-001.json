{
  "schema_version": "continuityops-task-policy-v1",
  "id": "COP-001",
  "title": "Prepare ContinuityOps cloud folder and orchestration model",
  "risk": "medium",
  "mode": "repo_only",
  "phase": 0,
  "slice": "S0",
  "depends_on": [],
  "allowed_paths": [
    "continuityops/**",
    ".github/workflows/continuityops-*.yml"
  ],
  "adapter_owned_paths": [
    "continuityops/evidence/manifests/folder-ready.json"
  ],
  "forbidden_operations": [
    "edit project-a",
    "edit project-c"
  ],
  "validators": [
    { "id": "path_scope", "timeout_seconds": 30, "required": true },
    { "id": "secret_scan", "timeout_seconds": 60, "required": true },
    { "id": "independence_lock", "timeout_seconds": 30, "required": true },
    { "id": "partition_manifest", "timeout_seconds": 60, "required": true },
    { "id": "orchestration_model", "timeout_seconds": 30, "required": true },
    { "id": "project_a_untouched", "timeout_seconds": 30, "required": true }
  ],
  "approval": {
    "required": false,
    "gate_id": "none",
    "approver": "human",
    "reason": "Operator disabled human gates while building."
  },
  "expected_artifacts": [
    "continuityops/PLAN.md",
    "continuityops/harness/policies/orchestration-model.json",
    "continuityops/integration/upstreams.lock.json"
  ],
  "expected_evidence": "continuityops/evidence/manifests/folder-ready.json",
  "claims": {
    "cloud_validated": false,
    "aws_implemented": false,
    "azure_implemented": false
  }
}
