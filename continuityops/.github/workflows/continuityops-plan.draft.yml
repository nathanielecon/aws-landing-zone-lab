# SUPERSEDED — promoted to repo-root:
#   .github/workflows/continuityops-terraform.yml
# Role: arn:aws:iam::<AWS_ACCOUNT_ID>:role/continuityops-gha
# Bootstrap: continuityops/terraform/ci-bootstrap/bootstrap-oidc-cloudshell.sh
#
# Kept for partition history only. Do not copy REPLACE_ME placeholders forward.
name: continuityops-terraform-plan-DRAFT-SUPERSEDED
on:
  workflow_dispatch:
jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Use .github/workflows/continuityops-terraform.yml"
          exit 1
