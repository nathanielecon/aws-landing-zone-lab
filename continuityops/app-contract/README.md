# ContinuityOps application contract

Pinned application/release interface consumed by ContinuityOps runtimes.

Until Project C exports an immutable digest (`OPEN-COP-001`), this directory
holds ContinuityOps-side contract stubs only. Any image built here is a
**ContinuityOps lab artifact**, not a Project C production release.
