# Specialty Orchestrator tooling moved

The Cursor UI Orchestrator → Codex Cloud helpers now live in:

**https://github.com/nathanielecon/cloud-tools**

Use that repo for:

- `Publish-CodexAuthRuntimeSecret.ps1`
- `Dispatch-CodexCloudWorker.ps1`
- `ORCHESTRATOR_UI_PROMPT.md`
- auth install / write-back / smoke scripts

This `cloud` repository keeps harness / Landing Zone lab content only.
